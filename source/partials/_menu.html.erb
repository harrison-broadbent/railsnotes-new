<% pages = {
    home: "/",
		blog: "/blog",
    about: "/about",
  } %>
<div class="flex items-center gap-x-4 mb-8">
  <% pages.each do |name, path|%>
    <%= link_to name, path, class: "text-stone-500 no-underline hover:text-stone-700" %>
  <% end%>
</div>
